# This service uses the global environment file from the parent project
# The following variables are automatically loaded from the global .env:

# LLM API Keys (already configured in global env)
# OPENAI_API_KEY=your_openai_api_key_here
# GOOGLE_API_KEY=your_google_api_key_here
# OPENROUTER_API_KEY=your_openrouter_api_key_here
# DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Supabase Configuration (already configured in global env)
# SUPABASE_URL=your_supabase_url
# SUPABASE_ANON_KEY=your_supabase_anon_key

# Storage Configuration (already configured in global env)
# STORAGE_PATH=./app/storage

# Development Settings (already configured in global env)
# DEBUG=false
# RELOAD=true
# DEVELOPMENT_MODE=true
# LOG_LEVEL=INFO

# LLM Service Specific Overrides (optional)
# OPENAI_MODEL=gpt-5
# GEMINI_MODEL=gemini-2.5-pro
# DEEPSEEK_MODEL=deepseek/deepseek-chat-v3.1
# DEFAULT_MAX_TOKENS=2000
# DEFAULT_TEMPERATURE=0.3